# ملاحظات تطوير المشروع

## 🐳 تحديات Docker
خلال إعداد بيئة المشروع باستخدام Docker، تعلمت النقاط التالية:
- يجب نسخ ملفات المشروع إلى المسار `/var/www/html/` داخل الحاوية لأن هذا هو المسار الافتراضي لخادم Apache.
- استخدمت ملف `.dockerignore` لاستبعاد الملفات غير الضرورية (مثل ملفات Git) لتقليل حجم الصورة (Image).

## 📁 تخزين البيانات
بما أن المشروع لا يستخدم قاعدة بيانات خارجية (SQL)، اعتمدت على ملف `tasks.json`:
- **التحدي:** كان يجب التأكد من أن الكود يمتلك صلاحية القراءة والكتابة على الملف.
- **الحل:** قمت ببرمجة الكود بحيث ينشئ الملف تلقائياً عند التشغيل الأول لضمان توافق الصلاحيات داخل الحاوية.

## ⚙️ منهجية العمل (Git)
اتبعت أسلوبcommits منظمة لفصل مراحل العمل:
1. إضافة كود التطبيق الأساسي.
2. إضافة إعدادات Docker.
3. إضافة التوثيق والملاحظات.
هذا يجعل مراجعة الكود أسهل وأكثر احترافية.